# Crypto Tool - dependances d'execution
PyQt6>=6.4.0
cryptography>=41.0.0

# Dependances de construction Windows uniquement, installees
# automatiquement dans .venv-build par build_windows.ps1 :
#   pyinstaller>=6.0.0
#   pillow>=10.0.0
